{
  "name": "rn-iso-monorepo",
  "private": true,
  "description": "Workspace root. The published packages live in packages/.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspace rn-iso && npm test --workspace @rn-iso/expo-build-cache"
  }
}
