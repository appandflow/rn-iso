# 1.0.0-rc.15

If Stim is not installed globally, replace `stim` below with `npx stim-cli`.

## New

- `stim worktree warm` copies missing ignored dependencies, native output, and local configuration from the main checkout into an existing linked worktree. Existing entries and tracked files are preserved. ([6412169](https://github.com/appandflow/stim/commit/64121692796dacd0b24e90fe2115346f49d42a46))
- `stim reload [ios|android]` reloads JavaScript on a running owned simulator or emulator without rebuilding or reinstalling the app. It reports the requested reload, selected device, Metro port, and strategy; app-side completion still needs verification. ([43da17e](https://github.com/appandflow/stim/commit/43da17e5e94fc920fdce65492432ee9f67411b28), [24d6d71](https://github.com/appandflow/stim/commit/24d6d714a6a9b0d934dc36eab26afcb8b6b1ea01))
- Sectioned guide topics support `stim guide <topic> <section>`, including direct error-code lookup such as `stim guide errors STIM_NO_METRO`. ([d377e92](https://github.com/appandflow/stim/commit/d377e92aa3b69af33cca8372c3b88f3a0eadde74))
- Owned Android emulators use Quick Boot where supported. Shutdown waits for the emulator and snapshot save to finish, and uncertain process locks prevent deletion. Displayless Linux keeps snapshots disabled. ([e6ddc3a](https://github.com/appandflow/stim/commit/e6ddc3a473c8a4c435aa11115ed531d0ab6883b9))

## Removed (breaking)

- `worktree create`, `--carry-ignored`, and creation-only settings are removed. Use Git to create worktrees, then run `stim worktree warm` when you need ignored files from main. Tracked edits are no longer copied. ([834b338](https://github.com/appandflow/stim/commit/834b3386d703bc950fa4258f6f39ccf6b915fc37))

## Fixes

- Warming preserves APFS clones through file publication on macOS, avoiding full data copies while keeping existing files safe. Files remain independent of the main checkout. ([0a5c2fa](https://github.com/appandflow/stim/commit/0a5c2faa27332b1b32c40a183462d9f53979b4c2))
- Benchmark overview links open the selected platform's audit, including Luna's Android JavaScript result. ([5d3aced](https://github.com/appandflow/stim/commit/5d3aced73be1d24602b29a8598424a740f97b056))
- Android shutdown flushes guest writes before killing the emulator, with a five-second limit so a stalled flush cannot block cleanup. ([3abbb87](https://github.com/appandflow/stim/commit/3abbb87f19a51e402c01831d172a917ed3ee466d))
- iOS skips cache lookup and storage when the final fingerprint cannot be computed, preventing an artifact from being stored under its earlier key. ([74d9a3e](https://github.com/appandflow/stim/commit/74d9a3e813a162c918cc1b6d39cfd430c0409897))
- Commands waiting for a native build keep waiting when another process takes over a failed builder's lock. ([70f3108](https://github.com/appandflow/stim/commit/70f31087f54b73948c916267814e9fb24ca6b513))
- Android Debug builds target the selected device's ABI and route eligible CMake compilation through ccache. ([0e5eb6d](https://github.com/appandflow/stim/commit/0e5eb6d0dd9d558835abb2fb5c77fe1025f2a970), [b9743cd](https://github.com/appandflow/stim/commit/b9743cd32a76dbd979dd1b54417a66757d30830e))
- `start`, `ios`, and `android` reject directories that are not React Native or Expo apps; `doctor` reports the problem as a finding. Invalid relative registry paths are reported as invalid records, and worktree Git commands pass paths as arguments. ([360d871](https://github.com/appandflow/stim/commit/360d871c19e8e54bffe86a21cdc2fadae947e9da), [c93997f](https://github.com/appandflow/stim/commit/c93997f107586616eab709cf0f86149e70fdd1f3), [b7100dd](https://github.com/appandflow/stim/commit/b7100ddf0bb88895cc300e1c64c36bd4f2369754))

## Docs

- Native cache QA uses a normally built disposable source checkout and separate empty test caches. ([8865207](https://github.com/appandflow/stim/commit/8865207666a314299e2642c6f15ceacf6fee2c65))
- The agent workflow verifies UI changes on the reported device and checks logs again before cleanup. ([0dff4a2](https://github.com/appandflow/stim/commit/0dff4a21397a26cf8a94210400cf143a276553d5))

- Error-guide headings match emitted messages, and shared-lock guidance covers the records those locks protect. ([8576b5e](https://github.com/appandflow/stim/commit/8576b5ebc3779ecab082fe9543fc9d4ea23be7dd))
- Setup, platform requirements, device ownership, and reload guidance now agree across the CLI and website. A successful log query is distinguished from a clean log result. ([b264ffa](https://github.com/appandflow/stim/commit/b264ffa7ef20e1b9cd4bc7d51ad2413cd818458e), [0ab7174](https://github.com/appandflow/stim/commit/0ab717450ac717cb552e33e88228dbc01646914d), [cc34d57](https://github.com/appandflow/stim/commit/cc34d57d7ba0e4ef52d969032c3e2b997ba995eb))
- The skill description names common agent tasks while retaining its single version-matched guide command. ([82e7ac9](https://github.com/appandflow/stim/commit/82e7ac9d50c0505f5931e9129d80201431839506))
- The benchmark website has separate platform/model selectors, visible unavailable combinations, and published Android results. ([ebe5359](https://github.com/appandflow/stim/commit/ebe5359152973c232bdd2588a410e124c936726a), [9c54814](https://github.com/appandflow/stim/commit/9c54814c3d951860cfba79b1dcda6f1ea40b7722), [8fde8cc](https://github.com/appandflow/stim/commit/8fde8ccfdfce84758058e9a92051785f75a5a8a8))

## Migration notes

`stim worktree create` and `--carry-ignored` are removed. Create the checkout with Git, then warm it from inside the new directory:

```sh
git worktree add -b my-feature ../my-feature HEAD
cd ../my-feature
stim worktree warm
```

If an agent harness already created the worktree, run only `stim worktree warm`. Warming never applies the main checkout's tracked edits. Remove `worktreeDir`, `worktree.baseRef`, `worktree.include`, and `.worktreeinclude` from project setup; `worktree.exclude` and `.worktreeexclude` still control warming. `stim worktree remove` works with warmed and unwarmed worktrees and retains Git-created branches.

`stim guide errors`, `lifecycle`, `facts`, and `cleanup` now print a section index. Add the section name to read its full guidance. The agent, Metro, logs, and settings topics remain whole. ([d377e92](https://github.com/appandflow/stim/commit/d377e92aa3b69af33cca8372c3b88f3a0eadde74))

The website build retains two accepted high-severity `image-size` denial-of-service advisories: [GHSA-5p2g-fcmc-qvqq](https://github.com/advisories/GHSA-5p2g-fcmc-qvqq) and [GHSA-w3rx-r6r6-pgpr](https://github.com/advisories/GHSA-w3rx-r6r6-pgpr). A malformed local image can hang a website build. The audit exceptions do not fix the parser; published Stim packages do not depend on it. Docusaurus tracks a replacement in [#12231](https://github.com/facebook/docusaurus/issues/12231).

The remaining native-device validation continues after this candidate is published.
